# Production Environment Variables
# Used when running: npm run build && npm run preview
# Or deployed to production server

# API Gateway URL - CHANGE THIS to your production API Gateway URL
VITE_API_GATEWAY=https://api.academihub.site

# Alternative: If API Gateway is on same domain with different path
# VITE_API_BASE=/api

# LiveKit (if using LiveKit Cloud)
VITE_LIVEKIT_URL=wss://elearning-microservice-98bdertb.livekit.cloud

# Production mode
NODE_ENV=production
